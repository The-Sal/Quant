from setuptools import setup

req = open('requirements.txt').read().splitlines()

setup(
    name='Quant',
    version='',
    packages=['quant'],
    url='',
    license='',
    author='',
    author_email='',
    description='',
    install_requires=req
)
